{
  "name": "llm-now",
  "version": "0.1.0",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "bin": {
    "llm-now": "./index.ts"
  },
  "scripts": {
    "start": "bun index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "runtime:smoke": "bun tests/runtime-compile-smoke.ts",
    "check": "bun test && bun run typecheck && bun run runtime:smoke"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "5.9.3"
  },
  "dependencies": {
    "@swartzrock/byok-runtime": "1.1.0"
  }
}
